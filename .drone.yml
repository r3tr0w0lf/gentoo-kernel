kind: pipeline
type: docker
name: Build Gentoo Kernel

platform:
  os: linux
  arch: amd64

clone:
  depth: 1
  
steps:
- name: Build
  image: x0rzavi/gentoo-kernel:latest
  commands:
    - bash build.sh
    
- name: Upload
  image: x0rzavi/gentoo-kernel:latest
  commands:
    - bash upload.sh
